#WSTĘP
"sgb polish ladders" - polska wersja programu stworzonego przez legendarnego Knuth'a opartego na oryginalnym kodzie źródłowym http://www-cs-faculty.stanford.edu/~uno/sgb.html 
Słownik polski został przygotowany przez dr. W. Bzyla mozna i jest on dostępny pod adresem: git://sigma.ug.edu.pl/~wbzyl/sgb.git Program NIE obsługuje polskich liter.

#UŻYCIE
Po pobraniu wszystkich plików należy uruchomić skrypt poprzez komende "./install", nastepnie wchodzimy do folderu (domyślnie cd sgb jeżeli plik install nie został zmodyfikowany) i używamy komend "make polishladders" a następniee "./polis_ladders" voila gotowe, można już korzystać z programu.

#DLA LENIWYCH
./install
cd sgb
make polishladders
./polish_ladders
